<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Enviar_ubicacion" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Cambiar el color de la barra de notificaciones -->
        <item name="colorPrimary">@color/personal2</item>
        <item name="android:statusBarColor">@color/personal1</item>
        <!-- Cambiar el color de fábrica de los botones -->
        <item name="android:colorButtonNormal">@color/personal1</item>

    </style>

    <style name="Theme.Enviar_ubicacion" parent="Base.Theme.Enviar_ubicacion" />
</resources>